<div class="jumbotron center">
	<% if user_signed_in? %>
		<h1>Hello, authenticated user!</h1>
	<% else %>
		<h1>Pinteresting</h1>
		<p class="lead">
			<%= link_to "Log In", new_user_session_path, class: 'btn btn-default btn-default btn-lg' %>
			<%= link_to "Sign Up", new_user_registration_path, class: 'btn btn-primary btn-default btn-lg' %>
		</p>
	<% end %>


<div>
